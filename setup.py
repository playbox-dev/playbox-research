from setuptools import setup, find_packages

setup(
    name='pbr',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        'opencv-python',  # Add other dependencies as needed
    ],
    # Add other relevant package information here
)